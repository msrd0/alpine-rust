We first have to build against upstream triplets, which by default use crt-static
linking. This won't build though, so let's just disable it, we disable it in our
triplets too.
--- rustc-1.49.0-src/compiler/rustc_target/src/spec/linux_musl_base.rs.orig
+++ rustc-1.49.0-src/compiler/rustc_target/src/spec/linux_musl_base.rs
@@ -61,6 +61,6 @@
     base.crt_objects_fallback = Some(CrtObjectsFallback::Musl);

-    // These targets statically link libc by default
-    base.crt_static_default = true;
+    // Do not statically link libc by default
+    base.crt_static_default = false;
 
     base
